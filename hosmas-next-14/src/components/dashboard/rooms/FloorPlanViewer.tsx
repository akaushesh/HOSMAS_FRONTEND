import * as React from "react";
import { Paper } from "@mui/material";

export default function FloorPlanViewer():React.JSX.Element{
    return(
        <Paper sx={{width:1, p:3}} elevation={10}>
            FloorPlanViewer
        </Paper>
    )
}